import 'package:flutter/material.dart';

class ChatRoomCard extends StatelessWidget {
  const ChatRoomCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
